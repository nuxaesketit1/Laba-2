#include <iostream>
#include <cmath>
using namespace std;

int main() {
	setlocale(0, "");

	double x1, x2, y1, y2, a, b;

	cout << "Введите координаты 1-й точки : " << endl;
	cin >> x1 >> y1;

	cout << "Введите координаты 2-ой точки : " << endl;
	cin >> x2 >> y2;

	a = (x1 - x2) < 0 ? -1 * (x1 - x2) : (x1 - x2);
	b = (y1 - y2) < 0 ? -1 * (y1 - y2) : (y1 - y2);

	cout << "Периметр равен :" << 2(a + b) << endl;
	cout << "Площадь равна :" << a * b << endl;

	cout << endl;

	system("pause");
	
	return 0;

}
